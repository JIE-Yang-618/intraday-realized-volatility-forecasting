"""Generate point forecasts for selected models and schemes."""
from model.training.runner import train_dispatch, predict_under_scheme  # noqa: F401
print("Generating predictions (see executables for orchestration).")
